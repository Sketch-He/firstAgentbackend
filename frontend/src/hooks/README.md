# frontend/src/hooks 目录说明

## 目录职责

这个目录当前未正式启用，保留为将来放置偏底层、偏通用、但又不适合进入 `composables/` 的浏览器交互型 hook。

## 当前状态

- 暂无实际业务文件
- 当前前端核心状态逻辑集中在 `composables/`

## 协作约定

1. 如果后续开始在这里新增文件，必须同步更新本文件，写清楚它与 `composables/` 的边界。
2. 如果确定不会使用这个目录，也应在后续整理时移除并同步更新上层 README。
