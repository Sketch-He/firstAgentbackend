# Agent Demo 方案草案

## 1. 你的背景与当前目标

基于仓库内 `CLAUDE.md` 和 `LEARNING_ROADMAP.md`，我对你的情况理解如下：

- 你是前端工程师，JS/TS 熟练，Python 和 Agent 开发处于系统学习阶段。
- 你当前目标不是直接做复杂商业产品，而是做一个能上线、能演示、能写进简历、能反向逼自己补齐后端和部署能力的项目。
- 你已经完成了：
  - Python 环境安装
  - DeepSeek API 选型
  - 基础 LLM Demo 跑通
  - 手写 ReAct Agent 的第一版学习
- 你现在想做一个自己的 Demo 部署上线，先像 DeepSeek 网页端那样，只保留“问答窗口 + 基本能力”，需求先不定死，边做边收敛。

我认为这个目标是合理的，而且非常适合作为“前端转 AI 应用开发”的第一站。

---

## 2. 我对你这次需求的理解

这次你真正要的，不只是一个聊天页面，而是一个**最小可上线 AI 产品闭环**：

1. 你能自己写出来
2. 你能本地跑起来
3. 你能部署到公网
4. 你能给别人演示
5. 你能解释清楚里面的技术选型、请求链路、流式输出、环境变量、部署流程

所以这个 Demo 的价值，不在于“功能多”，而在于它能把下面这些东西真正串起来：

- Python 后端
- FastAPI 接口设计
- SSE 流式输出
- LLM API 调用
- 前端对话 UI
- 前后端联调
- 环境变量管理
- 公网部署
- Git 推送后自动发布

---

## 3. 推荐方向

### 推荐方案：先做一个“类 DeepSeek 的对话产品壳子”，后端按 Agent 方向预留扩展

这是我最推荐的起步方案。

原因：

- 它最符合你现在的目标：先上线一个像样的产品，而不是一开始就陷入复杂 Agent 编排。
- 它能最大化复用你的前端优势，先把 UI、流式体验、消息状态、错误处理做扎实。
- 它仍然是 Agent 路线，而不是偏题。因为后端接口、消息结构、工具调用能力都可以从一开始按 Agent 方式预留。
- 它最适合学习部署。一个“前端静态站点 + Python API 服务”的架构，是最典型、最容易上线、也最容易讲清楚的 AI 应用架构。

一句话概括：

> 第一版先做“AI Chat App”，第二版再把它推进成“Agent App”。

---

## 4. 不建议你第一版就做什么

下面这些方向不适合当前作为第一版：

- 一上来就做多 Agent
- 一上来就做 RAG
- 一上来就做数据库查询 + 图表分析 + 文件上传全套
- 一上来就做完整账号体系
- 一上来就做复杂权限、会话持久化、消息同步

原因不是这些不重要，而是它们会稀释你这次最关键的学习目标：

- 先学会把一个 AI 产品真正上线
- 先用 Python 独立写完一条完整服务链路
- 先把最小产品体验打通

---

## 5. 产品形态建议

### 5.1 第一版产品定义

产品名先不用定，先按“类 DeepSeek Web Chat”做。

第一版只做这些：

- 左侧或顶部一个简单产品标题
- 中间一个消息列表区域
- 底部一个输入框 + 发送按钮
- 支持多轮对话
- 支持流式输出
- 支持 Markdown 展示
- 支持代码块展示
- 支持“新对话 / 清空上下文”
- 支持错误提示和加载状态

### 5.2 第一版暂时不做

- 登录注册
- 数据库存档
- 会话跨设备同步
- 文件上传
- 知识库
- 工具执行面板
- 模型切换
- 多角色系统

这样做的核心原则是：

> 先把“像产品”这件事做出来，而不是把“像平台”这件事做出来。

---

## 6. 这个 Demo 应该学到什么

这个项目不只是为了展示界面，而是为了系统学习。

### Python 侧

- `FastAPI` 路由设计
- `Pydantic` 请求/响应模型
- 环境变量读取
- 异步接口
- `StreamingResponse` 或 SSE 输出
- 错误处理和日志
- 项目结构组织

### Agent / LLM 侧

- `messages` 上下文结构
- `system prompt` 的作用
- 流式输出和非流式输出的区别
- 工具调用的接口预留方式
- 从“普通聊天”演进到“Agent”的路径

### 部署侧

- 前后端如何拆分部署
- 环境变量如何在线上配置
- 域名和 HTTPS 的基本流程
- Git Push 后自动部署
- 如何看日志排查线上问题

---

## 7. 技术方案建议

## 7.1 前端

默认推荐：

- `Vue + Vite + TypeScript`

原因：

- 你本来就有前端经验
- Vite 启动快，适合做这种中小型 Demo

### 前端页面模块

- `ChatPage`
- `MessageList`
- `MessageBubble`
- `Composer`
- `Header`
- `ErrorBanner`

### 前端状态

先保持简单：

- 当前消息列表
- 当前输入内容
- 当前是否正在生成
- 当前错误信息

本地存储可以先只做浏览器 `localStorage`，不引入数据库。

---

## 7.2 后端

默认推荐：

- `Python 3.11`
- `FastAPI`
- `uvicorn`
- `openai` SDK（连接 DeepSeek 的 OpenAI 兼容接口）

### 第一版后端接口

- `GET /health`
  - 健康检查，部署后验证服务是否存活
- `POST /api/chat`
  - 非流式调试接口
- `POST /api/chat/stream`
  - 流式对话接口，供正式前端使用

### 建议的消息结构

```json
{
  "messages": [
    { "role": "system", "content": "你是一个有帮助的助手" },
    { "role": "user", "content": "你好" }
  ]
}
```

这和你当前学习里的 `messages` 结构完全一致，能把 Demo 和学习路线连上。

---

## 7.3 Agent 预留，但第一版不强上

虽然第一版先不做复杂 Agent，但后端要为 Agent 留出扩展位。

建议你把后端代码分层成：

- `api/`：接口层
- `services/llm.py`：普通 LLM 对话
- `services/agent.py`：后续 Agent 循环
- `schemas/`：请求响应模型
- `core/config.py`：环境变量和配置

这样第二版要加工具调用时，只需要：

- 新增工具定义
- 新增 agent loop
- 在 `/api/chat/stream` 中切换底层调用模式

而不是推翻第一版重做。

---

## 8. 功能路线图

## Phase A：最小可用版本

目标：先像 DeepSeek 网页端，能对话、能流式输出、能上线。

功能：

- 输入问题
- 返回回答
- 流式展示
- Markdown 渲染
- 清空会话
- 部署公网可访问

这是第一阶段必须完成的版本。

## Phase B：从 Chat 升级为轻量 Agent

目标：让 Demo 不只是聊天，而是开始有“动手能力”。

建议只加 1 到 2 个工具：

- `calculate`
- `web_search` 或 `generate_sample_data`

这样可以非常清楚地展示：

- LLM 自己决定是否调用工具
- 工具结果回填 messages
- 再继续推理输出最终答案

这时候你就真正进入“Agent Demo”了。

## Phase C：向简历项目靠拢

这一步再根据你后面兴趣决定方向：

- 数据分析 Agent
- 前端学习教练 Agent
- 简历优化 / 面试陪练 Agent

我认为你长期最值得做的，还是“数据分析 Agent”，因为它和你当前路线图最一致。

---

## 9. 部署方案建议

## 9.1 我推荐的第一条上线路线

先做“前后端分离部署”：

- 前端：Vercel
- 后端：Render

推荐原因：

- 成本低
- 上手快
- 对前端工程师友好
- 不需要你第一天就处理服务器运维
- GitHub 推送后自动部署，最适合学习现代发布流程

参考：

- Render FastAPI 部署文档：<https://render.com/docs/deploy-fastapi>
- Vercel Vite 文档：<https://vercel.com/docs/frameworks/frontend/vite>

### 这条路线能学到什么

- 前后端分别部署
- 线上环境变量配置
- 跨域配置
- 构建命令 / 启动命令
- 域名分配和线上调试

---

## 9.2 第二条路线：自己买服务器，用 Docker Compose 部署

这条路线更适合第二阶段。

推荐部署形态：

- 1 台 Linux 云服务器
- `Docker Compose`
- 前端静态文件 + Nginx 或 Caddy
- FastAPI 服务

这条路线更适合学习“传统上线能力”：

- Linux
- 反向代理
- HTTPS
- 进程和容器
- 域名解析
- 日志查看

参考：

- Docker Compose 文档：<https://docs.docker.com/compose/gettingstarted/>

### 我的建议

不要一开始就上这条路线。

更好的顺序是：

1. 先用 Vercel + Render 跑通第一次上线
2. 再用 Docker Compose 重做一次

这样你能同时学会：

- 现代 PaaS 部署
- 传统服务器部署

---

## 10. 我给你的产品方向建议

因为你现在“需求未定”，我给你 3 个方向：

### 方向 A：通用 AI 聊天 Demo

特点：

- 最容易做
- 最容易上线
- 最容易把部署链路跑通

缺点：

- 简历区分度一般
- 如果后续不继续做 Agent，会比较像普通套壳

### 方向 B：前端转 AI 学习教练

特点：

- 非常贴合你个人背景
- Prompt 容易设计
- 很容易讲故事

可以让它做的事：

- 回答“前端如何转 AI”
- 给学习计划
- 给项目建议
- 解释 Python / FastAPI / Agent 概念

缺点：

- 第一版还比较依赖 Prompt，本身 Agent 性较弱

### 方向 C：数据分析助手

特点：

- 最贴合你长期路线
- 后续很容易演进成 Agent
- 简历价值最高

缺点：

- 第一版如果直接上，会比普通聊天 Demo 重一些

### 我的结论

**最推荐的路线是：**

先按方向 A 的产品壳子启动，命名和定位上偏方向 B 或 C。

也就是：

- UI 和交互按“通用聊天产品”实现
- Prompt 和产品说明按“学习教练”或“数据分析助手”包装
- 后端代码按“未来支持 Agent 工具调用”设计

这个平衡最好。

---

## 11. 我建议的第一版定位

如果现在必须选一个，我建议你做：

### “AI Agent 学习助手” Demo

它的定位不是“无所不能的大模型”，而是：

> 一个帮助前端工程师学习 Python、FastAPI、Agent 开发和部署流程的对话助手。

为什么我推荐这个，而不是直接做“数据分析助手”：

- 你的真实学习过程就是最好的产品故事
- 你最容易持续补内容
- 你最容易判断回答好不好
- 第二版很容易加工具

后续升级也自然：

- v1：纯聊天学习助手
- v2：加 `calculate` / `web_search`
- v3：加“生成学习计划”“代码解释”“项目方案建议”
- v4：再演进到数据分析 Agent

---

## 12. 开发顺序建议

如果下一步开始正式做，我建议按这个顺序推进：

1. 定产品定位和名称
2. 搭前端聊天页面
3. 搭 FastAPI 基础服务
4. 打通 DeepSeek 非流式接口
5. 改成流式输出
6. 完成前后端联调
7. 增加错误处理和加载态
8. 本地跑通
9. 部署 Render + Vercel
10. 公网联调
11. 再考虑加第一个工具

这个顺序的重点是：

> 先交付一个可访问的产品，再扩功能。

---

## 13. 当前仓库的一个工程问题

开始正式开发前，建议先处理一个问题：

- 仓库里的 `demo.py`
- `agent_v1.py`
- `LEARNING_ROADMAP.md`

目前残留了 Git 冲突标记。

这不影响本次方案讨论，但在继续开发前最好先清掉，否则后续运行、提交和阅读都会受影响。

---

## 14. 这份方案对应的结论

本次不建议你直接做一个“复杂 Agent 产品”。

本次最合适的路线是：

1. 先做一个类 DeepSeek 的最小聊天产品
2. 用 Python + FastAPI + DeepSeek + SSE 跑通完整链路
3. 先完成一次公网部署
4. 再把后端从 Chat 升级为 Agent

这条路线最适合你当前阶段，也最符合你的三个目标：

- 学习 Agent 开发
- 学习 Python
- 学习上线部署流程

---

## 15. 我建议你下一步怎么选

如果你认可这份方案，下一步我建议我们直接进入“定产品定位”。

我建议你在下面 3 个定位里先选一个作为第一版：

- `AI Agent 学习助手`：最贴合你当前状态，我最推荐
- `数据分析助手`：最贴合长期简历方向
- `通用 AI 聊天 Demo`：最快上线

如果你不想现在选死，也可以：

- 前端壳子先按通用聊天做
- 后端 Prompt 先按“AI Agent 学习助手”做
- 后续再切到数据分析方向

这是当前最稳的路线。

---

## 16. 参考资料

以下资料用于支撑部署路线建议：

- Render FastAPI 文档：<https://render.com/docs/deploy-fastapi>
- Render Web Services 文档：<https://render.com/docs/web-services>
- Vercel Vite 文档：<https://vercel.com/docs/frameworks/frontend/vite>
- Vercel CLI 部署文档：<https://vercel.com/docs/projects/deploy-from-cli>
- Docker Compose Quickstart：<https://docs.docker.com/compose/gettingstarted/>